# Required
NANOGPT_API_KEY=your-nanogpt-key-here

# X API — either bearer token directly:
X_BEARER_TOKEN=your-bearer-token

# ...or consumer + API key (auto-exchanges for bearer token):
# X_CONSUMER_KEY=your-consumer-key
# X_API_KEY=your-api-key

# Optional — custom topic/domain (default: local AI / local LLMs)
# SCOUT_FOCUS=robotics
